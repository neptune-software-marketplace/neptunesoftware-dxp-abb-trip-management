if (typeof price === "undefined" || price === null || price === "") { return; }  
if (typeof  currency === "undefined" ||  currency === null ||  currency === "") { return; }  

if (currency === "GBP"){
    var formattedText = (price * 1.17).toFixed(2) + " EUR";
}

return formattedText;